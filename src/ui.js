export function escapeHtml(value) {
  return String(value ?? '')
    .replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;').replace(/'/g, '&#039;');
}

export function shell(title, content) {
  return `<!doctype html><html lang="en"><head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<meta name="robots" content="noindex,nofollow"><title>${escapeHtml(title)} · Raven Sharp Store Ops</title>
<style>
:root{color-scheme:dark;--bg:#070a11;--card:#111827;--card2:#182235;--line:#2c3950;--text:#f7f8fc;--muted:#aab5c8;--purple:#925cff;--purple2:#6537ca;--green:#3ddc97;--red:#ff7188;--gold:#ffd166}
*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at top,#202743 0,#080b12 45%);color:var(--text);font:16px/1.5 system-ui,-apple-system,Segoe UI,sans-serif;min-height:100vh}a{color:#cdbaff}.wrap{width:min(1100px,calc(100% - 24px));margin:auto;padding:24px 0 70px}header{display:flex;align-items:center;justify-content:space-between;gap:14px;flex-wrap:wrap;margin-bottom:20px}.brand{display:flex;gap:12px;align-items:center}.mark{width:46px;height:46px;display:grid;place-items:center;border-radius:14px;background:linear-gradient(145deg,var(--purple),#32205e);font-weight:900;box-shadow:0 12px 36px #6b3ad755}h1,h2,h3,p{margin-top:0}h1{font-size:clamp(1.4rem,4vw,2rem);margin-bottom:2px}h2{font-size:1.15rem}.muted{color:var(--muted)}.grid{display:grid;grid-template-columns:repeat(12,1fr);gap:16px}.card{grid-column:span 12;background:linear-gradient(180deg,var(--card2),var(--card));border:1px solid var(--line);border-radius:18px;padding:19px;box-shadow:0 18px 50px #0006}.half{grid-column:span 6}.third{grid-column:span 4}@media(max-width:800px){.half,.third{grid-column:span 12}.wrap{width:min(100% - 18px,1100px)}.card{padding:15px}}
.button,button{appearance:none;border:0;border-radius:12px;padding:11px 15px;background:linear-gradient(135deg,var(--purple),var(--purple2));color:white;font-weight:760;text-decoration:none;cursor:pointer;display:inline-flex;align-items:center;justify-content:center;min-height:44px}.secondary{background:#253047!important;border:1px solid #3d4b66!important}.danger{background:#542139!important;border:1px solid #7a304b!important}.good{background:#12372c!important;border:1px solid #1c6b4e!important}input,select,textarea{width:100%;border:1px solid #3c4964;border-radius:11px;background:#0c1320;color:white;padding:10px 12px;font:inherit}input,select{min-height:44px}textarea{min-height:150px;resize:vertical}label{display:block;font-weight:720;margin-bottom:6px}.field{margin-bottom:14px}.row{display:grid;grid-template-columns:repeat(12,1fr);gap:12px}.span3{grid-column:span 3}.span4{grid-column:span 4}.span6{grid-column:span 6}.span8{grid-column:span 8}.span12{grid-column:span 12}@media(max-width:680px){.span3,.span4,.span6,.span8{grid-column:span 12}}.actions{display:flex;gap:8px;flex-wrap:wrap;align-items:center}.pill{display:inline-flex;align-items:center;border-radius:999px;padding:5px 10px;background:#1e2a3d;border:1px solid #34435c;font-size:.82rem}.pill.good{color:#a9f4cf}.pill.draft{background:#3e3217;border-color:#826b26;color:#ffe19a}.notice,.error{padding:13px 15px;border-radius:14px;margin-bottom:15px}.notice{background:#12372c;border:1px solid #1b694d;color:#a9f4cf}.error{background:#401c2a;border:1px solid #80334d;color:#ffc3cf}.listing{background:#0d1421;border:1px solid var(--line);border-radius:14px;padding:14px;margin-top:10px}.listing-head{display:flex;justify-content:space-between;gap:10px}.listing h3{font-size:1rem;margin-bottom:5px}.tiny{font-size:.82rem}.empty{text-align:center;padding:24px;border:1px dashed #43516b;border-radius:14px;color:var(--muted)}code{overflow-wrap:anywhere;color:#e8dfff}.tabs{display:flex;gap:8px;flex-wrap:wrap;margin-bottom:14px}.tabs a{padding:8px 11px;border-radius:10px;background:#202b3f;text-decoration:none}.sectionchips{display:flex;flex-wrap:wrap;gap:7px}.asset{display:flex;justify-content:space-between;gap:10px;align-items:center;border:1px solid var(--line);border-radius:12px;padding:10px;margin-top:8px}.asset img{width:70px;height:70px;object-fit:cover;border-radius:9px}.help{font-size:.84rem;color:var(--muted);margin-top:5px}.warning{border-left:4px solid var(--gold);padding:10px 12px;background:#352f1b;border-radius:8px}.safe{border-left:4px solid var(--green);padding:10px 12px;background:#123127;border-radius:8px}.counter{text-align:right;color:var(--muted);font-size:.8rem}.sticky{position:sticky;bottom:10px;background:#101827ee;backdrop-filter:blur(10px);padding:11px;border:1px solid var(--line);border-radius:15px;margin-top:15px}.checkbox{display:flex;gap:9px;align-items:center}.checkbox input{width:auto;min-height:0}.searchline{display:grid;grid-template-columns:1fr auto;gap:8px}
</style></head><body><main class="wrap">${content}</main></body></html>`;
}

export function brand(subtitle) {
  return `<div class="brand"><div class="mark">RS</div><div><h1>Raven Sharp Store Ops</h1><div class="muted">${escapeHtml(subtitle)}</div></div></div>`;
}

export function setupPage(callbackUrl, missing) {
  return shell('Setup', `<header>${brand('Setup required')}</header><section class="card"><h2>Cloudflare secrets needed</h2><div class="error">${missing.map(escapeHtml).join(' · ')}</div><ol><li>Open the Worker in Cloudflare.</li><li>Go to <strong>Settings → Variables and Secrets</strong>.</li><li>Add each name as an encrypted secret.</li><li>Use a private random phrase of at least 32 characters for <code>SESSION_SECRET</code>.</li></ol><h3>Exact Etsy callback URL</h3><p><code>${escapeHtml(callbackUrl)}</code></p><p class="muted tiny">Add this exact address in the Etsy developer app. Do not add a trailing slash.</p></section>`);
}

export function disconnectedPage(callbackUrl, notice = '', error = '') {
  return shell('Connect Etsy', `<header>${brand('Private seller dashboard')}</header>${messageBoxes(notice,error)}<div class="grid"><section class="card half"><span class="pill draft">Draft-safe</span><h2 style="margin-top:12px">Connect your Etsy shop</h2><p class="muted">Create and edit draft listings, upload images and digital files, organise sections, and update metadata. This app contains no publish action.</p><a class="button" href="/auth/start">Connect Etsy securely</a></section><section class="card half"><h2>Callback URL</h2><p><code>${escapeHtml(callbackUrl)}</code></p><p class="muted tiny">It must exactly match the callback registered in Etsy.</p></section></div>`);
}

export function dashboardPage({ shop, sections, listings, csrf, notice, error }) {
  const groups = ['active','draft','inactive','expired','sold_out'].map(state => ({state, listings:listings.filter(x=>x.state===state)}));
  return shell(shop.shop_name || 'Dashboard', `<header>${brand(shop.shop_name || 'Connected Etsy shop')}<div class="actions"><span class="pill good">● Connected</span><a class="button" href="/listings/new">New digital draft</a><a class="button secondary" href="/">Refresh</a><form method="post" action="/logout"><input type="hidden" name="csrf" value="${escapeHtml(csrf)}"><button class="danger">Disconnect</button></form></div></header>${messageBoxes(notice,error)}<div class="grid"><section class="card half"><h2>Add a shop section</h2><form method="post" action="/sections/create"><input type="hidden" name="csrf" value="${escapeHtml(csrf)}"><div class="searchline"><input name="title" maxlength="24" placeholder="Kids Workbooks" required><button>Create</button></div></form></section><section class="card half"><h2>Current sections</h2><div class="sectionchips">${sections.length?sections.map(s=>`<span class="pill">${escapeHtml(s.title)} · ${Number(s.active_listing_count||0)}</span>`).join(''):'<span class="muted">No sections yet.</span>'}</div></section><section class="card"><div class="listing-head"><div><h2>Listings</h2><p class="muted">Edit content, tags, price, files, images, and sections. Publication state is never sent to Etsy.</p></div><span class="pill">${listings.length} total</span></div>${groups.map(g=>listingGroup(g,sections,csrf)).join('')||'<div class="empty">No listings found.</div>'}</section></div>`);
}

function listingGroup(group, sections, csrf) {
  if (!group.listings.length) return '';
  const label = group.state.replace('_',' ').replace(/^./,c=>c.toUpperCase());
  return `<h3 style="margin-top:22px">${escapeHtml(label)} <span class="pill ${group.state==='draft'?'draft':''}">${group.listings.length}</span></h3>${group.listings.map(l=>listingCard(l,sections,csrf)).join('')}`;
}

function listingCard(listing, sections, csrf) {
  return `<article class="listing"><div class="listing-head"><div><h3>${escapeHtml(listing.title||`Listing ${listing.listing_id}`)}</h3><span class="muted tiny">ID ${escapeHtml(listing.listing_id)} · ${escapeHtml(listing.listing_type||'')}</span></div><span class="pill ${listing.state==='draft'?'draft':''}">${escapeHtml(listing.state)}</span></div><div class="actions" style="margin-top:12px"><a class="button" href="/listings/${encodeURIComponent(listing.listing_id)}/edit">Edit everything</a>${sections.length?`<form method="post" action="/listings/move" class="actions"><input type="hidden" name="csrf" value="${escapeHtml(csrf)}"><input type="hidden" name="listing_id" value="${escapeHtml(listing.listing_id)}"><select name="section_id" required style="width:auto"><option value="">Move to section</option>${sections.map(s=>`<option value="${escapeHtml(s.shop_section_id)}"${String(s.shop_section_id)===String(listing.shop_section_id)?' selected':''}>${escapeHtml(s.title)}</option>`).join('')}</select><button class="secondary">Move</button></form>`:''}</div></article>`;
}

export function listingFormPage({ mode, listing, sections, taxonomy, csrf, images = [], files = [], notice='', error='' }) {
  const isEdit = mode === 'edit';
  const action = isEdit ? `/listings/${encodeURIComponent(listing.listingId)}/update` : '/listings/create';
  const stateLabel = listing.state || 'draft';
  return shell(isEdit?'Edit listing':'New digital draft', `<header>${brand(isEdit?'Listing editor':'Create a digital draft')}<div class="actions"><a class="button secondary" href="/">Dashboard</a>${isEdit?`<span class="pill ${stateLabel==='draft'?'draft':''}">${escapeHtml(stateLabel)}</span>`:'<span class="pill draft">Will save as draft</span>'}</div></header>${messageBoxes(notice,error)}<div class="safe"><strong>Publish lock:</strong> this form never sends <code>state=active</code>. New listings remain drafts, and editing an active listing changes only the fields you save.</div><form method="post" action="${action}" style="margin-top:16px"><input type="hidden" name="csrf" value="${escapeHtml(csrf)}"><div class="grid"><section class="card"><h2>Search-facing content</h2><div class="field"><label>Title</label><input name="title" maxlength="140" required value="${escapeHtml(listing.title)}"><div class="counter">140 characters maximum</div></div><div class="field"><label>Description</label><textarea name="description" required>${escapeHtml(listing.description)}</textarea><div class="help">Put the strongest buyer benefit and exactly what is included near the top.</div></div><div class="row"><div class="field span6"><label>Keywords / Etsy tags</label><textarea name="tags" style="min-height:100px" placeholder="printable workbook, kids maths, homeschool resource">${escapeHtml(listing.tags)}</textarea><div class="help">Comma separated, up to 13 tags. The app removes duplicates and extras.</div></div><div class="field span3"><label>Materials</label><textarea name="materials" style="min-height:100px" placeholder="digital PDF, printable">${escapeHtml(listing.materials)}</textarea></div><div class="field span3"><label>Styles</label><textarea name="styles" style="min-height:100px" placeholder="colourful, educational">${escapeHtml(listing.styles)}</textarea></div></div></section><section class="card"><h2>Category and shop placement</h2><div class="row"><div class="field span8"><label>Category</label><input name="taxonomy_id" list="taxonomy-list" required value="${escapeHtml(listing.taxonomyId)}" placeholder="Type a category name, then choose it"><datalist id="taxonomy-list">${taxonomy.map(t=>`<option value="${escapeHtml(t.id)}">${escapeHtml(t.path)}</option>`).join('')}</datalist><div class="help">The saved value is Etsy's numeric category ID. Start typing a category name to search the suggestions.</div></div><div class="field span4"><label>Shop section</label><select name="section_id"><option value="">No section</option>${sections.map(s=>`<option value="${escapeHtml(s.shop_section_id)}"${String(s.shop_section_id)===String(listing.sectionId)?' selected':''}>${escapeHtml(s.title)}</option>`).join('')}</select></div></div></section><section class="card"><h2>Price and item details</h2><div class="row"><div class="field span3"><label>Price</label><input name="price" type="number" min="0.20" step="0.01" required value="${escapeHtml(listing.price)}"></div><div class="field span3"><label>Quantity</label><input name="quantity" type="number" min="1" max="999" required value="${escapeHtml(listing.quantity)}"></div><div class="field span3"><label>Who made it?</label><select name="who_made"><option value="i_did"${listing.whoMade==='i_did'?' selected':''}>I did</option><option value="collective"${listing.whoMade==='collective'?' selected':''}>A member of my shop</option><option value="someone_else"${listing.whoMade==='someone_else'?' selected':''}>Another company or person</option></select></div><div class="field span3"><label>When made?</label><select name="when_made">${whenMadeOptions(listing.whenMade)}</select></div></div><div class="row"><div class="field span4"><label class="checkbox"><input type="checkbox" name="should_auto_renew" value="true"${listing.shouldAutoRenew?' checked':''}> Auto renew</label></div><div class="field span4"><label class="checkbox"><input type="checkbox" name="is_taxable" value="true"${listing.isTaxable?' checked':''}> Taxable</label></div><div class="field span4"><label class="checkbox"><input type="checkbox" name="is_supply" value="true"${listing.isSupply?' checked':''}> Craft supply</label></div></div><input type="hidden" name="type" value="${escapeHtml(listing.type || 'download')}"></section></div><div class="sticky actions"><button type="submit">${isEdit?'Save listing changes':'Create draft listing'}</button><a class="button secondary" href="/">Cancel</a><span class="muted tiny">No publish action exists in this app.</span></div></form>${isEdit?assetPanels(listing,images,files,csrf):''}`);
}

function assetPanels(listing, images, files, csrf) {
  return `<div class="grid" style="margin-top:16px"><section class="card half"><h2>Listing images</h2><p class="muted">Upload JPG, PNG, GIF, SVG, or HEIC images. Etsy supports up to 20 listing photos.</p><form method="post" enctype="multipart/form-data" action="/listings/${encodeURIComponent(listing.listingId)}/images/upload"><input type="hidden" name="csrf" value="${escapeHtml(csrf)}"><div class="field"><input type="file" name="image" accept="image/jpeg,image/png,image/gif,image/svg+xml,image/heic" required></div><div class="field"><label>Image rank</label><input type="number" name="rank" min="1" max="20" value="${Math.min(images.length+1,20)}"></div><button>Upload image</button></form>${images.length?images.map(img=>`<div class="asset"><div class="actions"><img src="${escapeHtml(img.url_170x135||img.url_75x75||'')}" alt=""><div><strong>Image ${escapeHtml(img.rank||'')}</strong><div class="muted tiny">ID ${escapeHtml(img.listing_image_id)}</div></div></div></div>`).join(''):'<div class="empty" style="margin-top:12px">No images returned by Etsy.</div>'}</section><section class="card half"><h2>Digital download files</h2><p class="muted">Up to 5 files, maximum 20MB each. Buyers see the uploaded filenames.</p><form method="post" enctype="multipart/form-data" action="/listings/${encodeURIComponent(listing.listingId)}/files/upload"><input type="hidden" name="csrf" value="${escapeHtml(csrf)}"><div class="field"><input type="file" name="file" required></div><div class="field"><label>File rank</label><input type="number" name="rank" min="1" max="5" value="${Math.min(files.length+1,5)}"></div><button>Upload digital file</button></form>${files.length?files.map(file=>`<div class="asset"><div><strong>${escapeHtml(file.filename||file.name||'Digital file')}</strong><div class="muted tiny">${escapeHtml(file.filesize||file.size_bytes||'')} · rank ${escapeHtml(file.rank||'')}</div></div></div>`).join(''):'<div class="empty" style="margin-top:12px">No digital files attached yet.</div>'}</section></div>`;
}

function whenMadeOptions(selected) {
  const values = [['2020_2026','2020 to 2026'],['made_to_order','Made to order'],['2010_2019','2010 to 2019'],['2007_2009','2007 to 2009'],['2000_2006','2000 to 2006'],['before_2007','Before 2007'],['1990s','1990s'],['1980s','1980s'],['1970s','1970s'],['1960s','1960s'],['1950s','1950s'],['1940s','1940s'],['1930s','1930s'],['1920s','1920s'],['1910s','1910s'],['1900s','1900s'],['1800s','1800s'],['1700s','1700s'],['before_1700','Before 1700']];
  return values.map(([value,label])=>`<option value="${value}"${value===selected?' selected':''}>${label}</option>`).join('');
}

export function errorPage(title,message) {
  return shell(title,`<header>${brand(title)}</header><section class="card"><div class="error">${escapeHtml(message)}</div><a class="button" href="/">Return to dashboard</a></section>`);
}

function messageBoxes(notice,error) {
  return `${notice?`<div class="notice">${escapeHtml(notice)}</div>`:''}${error?`<div class="error">${escapeHtml(error)}</div>`:''}`;
}
